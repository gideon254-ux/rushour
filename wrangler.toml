# rushour - Cloudflare Pages + Workers Configuration

# For local development: npm run dev
# For local API testing: npx wrangler dev

# NOTE: Do NOT run "wrangler deploy" from Pages
# The API Worker should be deployed separately using: npx wrangler deploy
# Or via GitHub Actions
